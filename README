This is my cross-platform elisp repository.

After cloning it, you'll need to install the color-theme package from 
http://download-mirror.savannah.gnu.org/releases/color-theme/color-theme-6.6.0.tar.gz and install under the bentzelisp root.

Also, in homedir do ln -s bentzelisp/.emacs .emacs
